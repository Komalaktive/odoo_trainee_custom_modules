<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_patient_id_card" model="ir.actions.report">
        <field name="name">Patient ID Card</field>
        <field name="model">patient.info</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">hospital_management.report_patient_id_card</field>
        <field name="report_file">hospital_management.report_patient_id_card</field>
        <field name="print_report_name">'Patient Card - %s' % (object.name)</field>
        <field name="binding_model_id" ref="model_patient_info"/>
    </record>

</odoo>