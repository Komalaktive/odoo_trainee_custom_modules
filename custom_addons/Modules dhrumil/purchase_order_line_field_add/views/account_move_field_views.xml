<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="account_move_field_id" model="ir.ui.view">
        <field name="name">account.move.field.id</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet/group/group[@id='header_right_group']/div[hasclass('d-flex')]" position="after">
                <field name="demo_field"/>
            </xpath>
        </field>
    </record>

</odoo>